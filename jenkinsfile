pipeline {
    agent any

    tools {
        maven 'Maven'
    }

    environment {
        SONAR_HOME = tool 'SonarQube'
    }

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/baseemibnbasheer8-dotcom/demo-project.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean compile'
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('SonarQube') {
                    sh 'mvn sonar:sonar'
                }
            }
        }

        stage('Quality Gate') {
            steps {
                waitForQualityGate abortPipeline: true
            }
        }
    }
}
